C Like LinkedList in Common Lisp

Requirements
=============
     (ql:quickload "iterate")

InitialSetup
============
      (defpackage meme (:use :cl :iterate))
      (in-package meme)
  
Compile and Load (C-c C-k) in SLIME
       enjoy
